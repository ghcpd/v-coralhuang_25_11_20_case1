{
  "name": "ui-interaction-bug-demo",
  "version": "1.0.0",
  "description": "Fixed UI interaction bugs with automated Playwright tests",
  "scripts": {
    "start": "nohup python3 -m http.server 3000 > /tmp/html.log 2>&1 & echo $! > /tmp/server.pid && echo Server started on port 3000",
    "stop": "if [ -f /tmp/server.pid ]; then kill $(cat /tmp/server.pid) && rm /tmp/server.pid; fi",
    "test": "npx playwright test",
    "test:ci": "npx playwright test --reporter=dot"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
